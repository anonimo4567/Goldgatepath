import React from "react";
import ReactDOM from "react-dom/client";
import GoldGateModernPage from "./components/GoldGateModernPage";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <GoldGateModernPage />
  </React.StrictMode>
);